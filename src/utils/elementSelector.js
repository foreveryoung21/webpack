export function selectElement(element){
    let el = document.querySelector(element);
    return el;

}


export function selectElementAll(element){
    let el = document.querySelectorAll(element);
    return el;

}
